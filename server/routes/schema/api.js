/**
 * A trip start request info
 * @typedef {object} TripStartRequest
 * @property {number} lat.required - Trip start latitude
 * @property {number} long.required - Trip end longitude
 */

/**
 * A trip end request info
 * @typedef {object} TripEndRequest
 * @property {number} lat.required - Trip start latitude
 * @property {number} long.required - Trip end longitude
 * @property {number} price.required - Trip price
 */

/**
 * A trip response info
 * @typedef {object} TripResponse
 * @property {string} id.required - Trip id
 */

